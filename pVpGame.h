#pragma once
#include <iostream>

using namespace std;
void pMenu(int &x);
bool shot(bool &player);
bool checking(bool &player);
void shipCheck(int x, int y, bool player);
bool majorCheck();
